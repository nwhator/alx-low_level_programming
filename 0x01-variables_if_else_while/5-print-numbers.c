#include <stdio.h>

/**
 * main - Entry point
 * Description - Numbers, counting them right?
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}

	return (0);
}
